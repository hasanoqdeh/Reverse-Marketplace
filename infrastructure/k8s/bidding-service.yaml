apiVersion: apps/v1
kind: Deployment
metadata:
  name: bidding-service
  namespace: backend
  labels:
    app: bidding-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bidding-service
  template:
    metadata:
      labels:
        app: bidding-service
    spec:
      containers:
      - name: bidding-service
        image: reverse-marketplace/bidding-service:latest
        ports:
        - containerPort: 3003
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "100m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 60
        readinessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: bidding-service
  namespace: backend
spec:
  selector:
    app: bidding-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3003
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: bidding-service-hpa
  namespace: backend
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: bidding-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
