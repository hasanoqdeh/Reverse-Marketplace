apiVersion: apps/v1
kind: Deployment
metadata:
  name: request-service
  namespace: backend
  labels:
    app: request-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: request-service
  template:
    metadata:
      labels:
        app: request-service
    spec:
      containers:
      - name: request-service
        image: reverse-marketplace/request-service:latest
        ports:
        - containerPort: 3001
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "100m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 60
        readinessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: request-service
  namespace: backend
spec:
  selector:
    app: request-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3001
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: request-service-hpa
  namespace: backend
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: request-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
