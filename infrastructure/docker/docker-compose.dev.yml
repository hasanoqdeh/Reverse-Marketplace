version: '3.8'

services:
  # Databases
  postgres:
    image: postgres:15-alpine
    container_name: reverse-marketplace-postgres
    environment:
      POSTGRES_DB: reverse_marketplace
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-scripts/02-postgres-init.sh:/docker-entrypoint-initdb.d/02-postgres-init.sh:ro
    networks:
      - reverse-marketplace-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  mongodb:
    image: mongo:7
    container_name: reverse-marketplace-mongodb
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: reverse_marketplace
    ports:
      - "27018:27017"
    volumes:
      - mongodb_data:/data/db
      - ./init-scripts/03-mongodb-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
    networks:
      - reverse-marketplace-network
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  cassandra:
    image: cassandra:4
    container_name: reverse-marketplace-cassandra
    ports:
      - "9042:9042"
    volumes:
      - cassandra_data:/var/lib/cassandra
    networks:
      - reverse-marketplace-network

  redis:
    image: redis:7-alpine
    container_name: reverse-marketplace-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - reverse-marketplace-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # Message Broker
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: reverse-marketplace-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: password
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - reverse-marketplace-network
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # Landing Page Service
  landing-page:
    build:
      context: ../../services/landing-page
      dockerfile: Dockerfile
    container_name: reverse-marketplace-landing
    environment:
      NODE_ENV: development
      PORT: 3007
    ports:
      - "3007:3007"
    volumes:
      - ../../services/landing-page:/app
      - /app/node_modules
    networks:
      - reverse-marketplace-network
    command: ["pnpm", "run", "start:dev"]

  # Admin Dashboard Service
  admin-dashboard:
    image: node:20-alpine
    container_name: reverse-marketplace-admin
    working_dir: /app
    environment:
      NODE_ENV: development
      PORT: 3008
      NEXT_PUBLIC_API_URL: http://kong:8000/api
    ports:
      - "3008:3008"
    volumes:
      - ../../apps/admin-dashboard:/app
      - /app/node_modules
    networks:
      - reverse-marketplace-network
    command: ["sh", "-c", "npm install && npm run dev"]

  # API Gateway
  kong:
    image: kong:3
    container_name: reverse-marketplace-kong
    environment:
      KONG_DATABASE: "off"
      KONG_DECLARATIVE_CONFIG: /kong/declarative/kong.yml
      KONG_PROXY_ACCESS_LOG: /dev/stdout
      KONG_ADMIN_ACCESS_LOG: /dev/stdout
      KONG_PROXY_ERROR_LOG: /dev/stderr
      KONG_ADMIN_ERROR_LOG: /dev/stderr
      KONG_ADMIN_LISTEN: 0.0.0.0:8001
    ports:
      - "8000:8000"
      - "8001:8001"
      - "8443:8443"
      - "8444:8444"
    volumes:
      - ./kong/kong.yml:/kong/declarative/kong.yml
    networks:
      - reverse-marketplace-network
    depends_on:
      - landing-page
      - admin-dashboard
      - identity-service

  # Microservices
  identity-service:
    build:
      context: ../../services/identity-service
      dockerfile: Dockerfile
    container_name: reverse-marketplace-identity
    environment:
      NODE_ENV: development
      PORT: 3000
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/reverse_marketplace
      REDIS_URL: redis://redis:6379
      REDIS_HOST: redis
      REDIS_PORT: 6379
      RABBITMQ_URL: amqp://admin:password@rabbitmq:5672
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USERNAME: admin
      RABBITMQ_PASSWORD: password
      JWT_SECRET: your-super-secret-jwt-key
      # Database initialization flags
      DB_MIGRATE: "true"
    # Internal port only (accessed through Kong)
    # ports:
    #   - "3000:3000"
    volumes:
      - ../../services/identity-service:/app
      - /app/node_modules

    networks:
      - reverse-marketplace-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    command: ["sh", "-c", "/app/scripts/wait-for-db.sh postgres && /app/scripts/wait-for-db.sh redis && /app/scripts/wait-for-db.sh rabbitmq && pnpm run start:dev"]



  request-service:
    build:
      context: ../../services/request-service
      dockerfile: Dockerfile
    container_name: reverse-marketplace-requests
    environment:
      NODE_ENV: development
      PORT: 3001
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/reverse_marketplace
      REDIS_URL: redis://redis:6379
      REDIS_HOST: redis
      REDIS_PORT: 6379
      RABBITMQ_URL: amqp://admin:password@rabbitmq:5672
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USERNAME: admin
      RABBITMQ_PASSWORD: password
      AWS_S3_BUCKET: reverse-marketplace-dev
      AWS_ACCESS_KEY_ID: your-access-key
      AWS_SECRET_ACCESS_KEY: your-secret-key
      # Database initialization flags
      DB_MIGRATE: "true"
    # Internal port only (accessed through Kong)
    # ports:
    #   - "3001:3001"
    volumes:
      - ../../services/request-service:/app
      - /app/node_modules

    networks:
      - reverse-marketplace-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    command: ["sh", "-c", "/app/scripts/wait-for-db.sh postgres && /app/scripts/wait-for-db.sh redis && /app/scripts/wait-for-db.sh rabbitmq && pnpm run start:dev"]



  matching-engine:
    build:
      context: ../../services/matching-engine
      dockerfile: Dockerfile
    container_name: reverse-marketplace-matching
    environment:
      NODE_ENV: development
      PORT: 3002
      REDIS_URL: redis://redis:6379
      REDIS_HOST: redis
      REDIS_PORT: 6379
      RABBITMQ_URL: amqp://admin:password@rabbitmq:5672
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USERNAME: admin
      RABBITMQ_PASSWORD: password
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/reverse_marketplace
      # Database initialization flags
      DB_MIGRATE: "true"
    # Internal port only (accessed through Kong)
    # ports:
    #   - "3002:3002"
    volumes:
      - ../../services/matching-engine:/app
      - /app/node_modules

    networks:
      - reverse-marketplace-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    command: ["sh", "-c", "/app/scripts/wait-for-db.sh postgres && /app/scripts/wait-for-db.sh redis && /app/scripts/wait-for-db.sh rabbitmq && pnpm run start:dev"]



  bidding-service:
    build:
      context: ../../services/bidding-service
      dockerfile: Dockerfile
    container_name: reverse-marketplace-bidding
    environment:
      NODE_ENV: development
      PORT: 3003
      MONGODB_URL: mongodb://admin:password@mongodb:27017/reverse_marketplace?authSource=admin
      MONGODB_URI: mongodb://admin:password@mongodb:27017/reverse_marketplace?authSource=admin
      MONGODB_HOST: mongodb
      MONGODB_PORT: 27017
      RABBITMQ_URL: amqp://admin:password@rabbitmq:5672
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USERNAME: admin
      RABBITMQ_PASSWORD: password
      # Database initialization flags
      DB_MIGRATE: "true"
    # Internal port only (accessed through Kong)
    # ports:
    #   - "3003:3003"
    volumes:
      - ../../services/bidding-service:/app
      - /app/node_modules

    networks:
      - reverse-marketplace-network
    depends_on:
      mongodb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    command: ["sh", "-c", "/app/scripts/wait-for-db.sh mongodb && /app/scripts/wait-for-db.sh rabbitmq && pnpm run start:dev"]


  notification-service:
    build:
      context: ../../services/notification-service
      dockerfile: Dockerfile
    container_name: reverse-marketplace-notifications
    environment:
      NODE_ENV: development
      PORT: 3004
      REDIS_URL: redis://redis:6379
      REDIS_HOST: redis
      REDIS_PORT: 6379
      RABBITMQ_URL: amqp://admin:password@rabbitmq:5672
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USERNAME: admin
      RABBITMQ_PASSWORD: password
      FCM_SERVER_KEY: your-fcm-server-key
      # Database initialization flags
      DB_MIGRATE: "true"
    # Internal port only (accessed through Kong)
    # ports:
    #   - "3004:3004"
    volumes:
      - ../../services/notification-service:/app
      - /app/node_modules

    networks:
      - reverse-marketplace-network
    depends_on:
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    command: ["sh", "-c", "/app/scripts/wait-for-db.sh redis && /app/scripts/wait-for-db.sh rabbitmq && pnpm run start:dev"]


  chat-service:
    build:
      context: ../../services/chat-service
      dockerfile: Dockerfile
    container_name: reverse-marketplace-chat
    environment:
      NODE_ENV: development
      PORT: 3005
      CASSANDRA_CONTACT_POINTS: cassandra:9042
      REDIS_URL: redis://redis:6379
      REDIS_HOST: redis
      REDIS_PORT: 6379
      RABBITMQ_URL: amqp://admin:password@rabbitmq:5672
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USERNAME: admin
      RABBITMQ_PASSWORD: password
      # Database initialization flags
      DB_MIGRATE: "true"
    # Internal port only (accessed through Kong)
    # ports:
    #   - "3005:3005"
    volumes:
      - ../../services/chat-service:/app
      - /app/node_modules

    networks:
      - reverse-marketplace-network
    depends_on:
      cassandra:
        condition: service_started
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    command: ["sh", "-c", "/app/scripts/wait-for-db.sh cassandra && /app/scripts/wait-for-db.sh redis && /app/scripts/wait-for-db.sh rabbitmq && pnpm run start:dev"]


  payment-service:
    build:
      context: ../../services/payment-service
      dockerfile: Dockerfile
    container_name: reverse-marketplace-payments
    environment:
      NODE_ENV: development
      PORT: 3006
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/reverse_marketplace
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      RABBITMQ_URL: amqp://admin:password@rabbitmq:5672
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USERNAME: admin
      RABBITMQ_PASSWORD: password
      STRIPE_SECRET_KEY: sk_test_your-stripe-key
      THAWANI_SECRET_KEY: your-thawani-key
      # Database initialization flags
      DB_MIGRATE: "true"
    # Internal port only (accessed through Kong)
    # ports:
    #   - "3006:3006"
    volumes:
      - ../../services/payment-service:/app
      - /app/node_modules

    networks:
      - reverse-marketplace-network
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    command: ["sh", "-c", "/app/scripts/wait-for-db.sh postgres && /app/scripts/wait-for-db.sh rabbitmq && pnpm run start:dev"]



volumes:
  postgres_data:
  mongodb_data:
  cassandra_data:
  redis_data:
  rabbitmq_data:

networks:
  reverse-marketplace-network:
    driver: bridge
